# Xcode derived data and build output
DerivedData/
build/
*.xcuserstate
xcuserdata/

# Release artifacts (ExportOptions.plist is committed — CI needs it, no secrets in it)
*.dmg

# Xcode user-specific workspace settings
*.xcworkspace/xcuserdata/

# macOS
.DS_Store

# Swift Package Manager
.swiftpm/
